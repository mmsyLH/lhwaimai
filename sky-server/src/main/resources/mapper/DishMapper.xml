<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.DishMapper">
    <!--useGeneratedKeys="true"
    获得数据插入所生成的主键值
        keyProperty="id" id是Dish的ID
    -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into dish(name, category_id, price, image, description, create_time, update_time, create_user, update_user,status)
        values
        (#{name}, #{categoryId}, #{price},#{image},
         #{description}, #{createTime}, #{updateTime},
         #{createUser}, #{updateUser},#{status})
    </insert>

    <!--SELECT d.*,c.name from dish d LEFT JOIN category c on d.category_id =c.id-->
    <select id="pageQuery" resultType="com.sky.vo.DishVO">
        SELECT d.*,c.name as categoryName from dish d LEFT JOIN category c on d.category_id =c.id
        <where>
            <if test="name!=null">
                and d.name like concat('%',#{name},'%')
            </if>
            <if test="categoryId!=null">
                and d.category_id like concat('%',#{categoryId},'%')
            </if>
            <if test="status!=null">
                and d.status like concat('%',#{status},'%')
            </if>
        </where>
        order by d.create_time desc
    </select>
    <!--在 MyBatis 中，parameterType 属性指定了传递给 SQL 语句的参数类型。
    在这个示例中，parameterType="Dish" 表示传递给 list SQL 语句的参数类型是 com.sky.entity.Dish 类型的对象。
    这个对象可以在 SQL 语句中使用，
    例如在 WHERE 子句中使用它的属性来过滤结果。如果 SQL 语句不需要参数，则可以省略 parameterType 属性。-->
    <select id="list" resultType="com.sky.entity.Dish" parameterType="Dish">
        select * from dish
        <where>
            <if test="name != null">
                and name like concat('%',#{name},'%')
            </if>
            <if test="categoryId != null">
                and category_id = #{categoryId}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
        </where>
        order by create_time desc
    </select>

    <update id="update">
        update dish
        <set>
            <if test="name!=null">name=#{name},</if>
            <if test="categoryId!=null">category_id=#{categoryId},</if>
            <if test="price!=null">price=#{price},</if>
            <if test="image!=null">image=#{image},</if>
            <if test="description!=null">description=#{description},</if>
            <if test="status!=null">status=#{status},</if>
            <if test="updateTime!=null">update_time=#{updateTime},</if>
            <if test="updateUser!=null">update_user=#{updateUser},</if>
        </set>
        where id=#{id}
    </update>
</mapper>
